Generate a Streamlit app code for an AI chatbot. The UI should include a simple text entry field where users can type their queries, and the chatbot should display responses accordingly. The code should be generated based on the following user specifications (provided in JSON format) and should only use file metadata, not the full file content. Include necessary comments to explain the code logic.
